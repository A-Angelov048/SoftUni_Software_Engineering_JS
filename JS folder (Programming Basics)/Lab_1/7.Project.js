function project(input){

    let architect = input[0];
    let numberOfProjects = Number(input[1] * 3);
    
    console.log(`The architect ${architect} will need ${numberOfProjects} hours to complete ${numberOfProjects / 3} project/s. `);

}

project(["George", "4"]);








//"The architect {името на архитекта} will need {необходими часове} hours to complete {брой на проектите} project/s."